{
  "name": "@restorecommerce/ostorage-srv",
  "version": "0.3.6",
  "description": "Restorecommerce microservice for storing objects",
  "main": "lib/start.js",
  "author": "n-fuse GmbH",
  "repository": {
    "type": "git",
    "url": "https://github.com/restorecommerce/ostorage-srv.git"
  },
  "license": "MIT",
  "keywords": [
    "restore",
    "commerce",
    "microservice",
    "ostorage",
    "srv"
  ],
  "dependencies": {
    "@restorecommerce/acs-client": "^1.1.4",
    "@restorecommerce/chassis-srv": "^1.2.1",
    "@restorecommerce/cluster-service": "^1.0.2",
    "@restorecommerce/grpc-client": "^2.0.1",
    "@restorecommerce/kafka-client": "^1.0.9",
    "@restorecommerce/logger": "^1.2.3",
    "@restorecommerce/rc-grpc-clients": "^3.1.0",
    "@restorecommerce/resource-base-interface": "^1.2.0",
    "@restorecommerce/service-config": "^1.0.5",
    "aws-sdk": "^2.1391.0",
    "koa-multer": "^1.0.2",
    "multer-s3": "^3.0.1",
    "s3-upload-stream": "^1.0.7",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@alenon/grpc-mock-server": "^3.0.20",
    "@grpc/proto-loader": "^0.7.7",
    "@restorecommerce/protos": "^3.1.0",
    "@types/lodash": "^4.14.195",
    "@types/mocha": "^10.0.1",
    "@types/node": "^20.2.5",
    "@typescript-eslint/eslint-plugin": "^5.59.9",
    "@typescript-eslint/parser": "^5.59.9",
    "coveralls": "^3.1.1",
    "cross-env": "^7.0.3",
    "eslint": "^8.42.0",
    "eslint-plugin-prefer-arrow-functions": "^3.1.4",
    "mocha": "^10.2.0",
    "nodemon": "^2.0.22",
    "npm-run-all": "^4.1.5",
    "nyc": "^15.1.0",
    "rimraf": "^5.0.1",
    "should": "^13.2.3",
    "sleep": "^6.3.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "start": "node lib/start.js",
    "dev": "cross-env NODE_ENV=development && nodemon -e ts --trace-warnings lib/start.js",
    "test": "npm run build && npm run lint && nyc npm run mocha",
    "lint": "eslint src --ext .ts",
    "mocha": "cross-env NODE_ENV=test mocha --full-trace --exit --timeout 15000",
    "tsctests": "tsc -d -p tsconfig.test.json",
    "test-debug": "cross-env NODE_ENV=test mocha -R spec test/test.js --inspect-brk",
    "lcov-report": "nyc report --reporter=lcov",
    "coveralls": "nyc report --reporter=text-lcov | coveralls",
    "build:tsc": "tsc -d",
    "build:clean": "rimraf lib",
    "build": "npm-run-all lint build:clean build:tsc"
  },
  "engines": {
    "node": ">= 18.0.0"
  }
}
