{
  "name": "@restorecommerce/ostorage-srv",
  "version": "0.0.1",
  "description": "Restore Commerce microservice for storing objects",
  "main": "lib/start.js",
  "author": "Invend GmbH",
  "repository": {
    "type": "git",
    "url": "https://github.com/restorecommerce/ostorage-srv.git"
  },
  "license": "MIT",
  "keywords": [
    "restore",
    "commerce",
    "microservice",
    "ostorage",
    "srv"
  ],
  "dependencies": {
    "@restorecommerce/acs-client": "https://github.com/restorecommerce/acs-client#acs-client-ctx-removed",
    "@restorecommerce/chassis-srv": "^0.1.2",
    "@restorecommerce/cluster-service": "^0.1.4",
    "@restorecommerce/grpc-client": "0.0.6",
    "@restorecommerce/kafka-client": "^0.1.10",
    "@restorecommerce/logger": "^0.1.16",
    "@restorecommerce/protos": "https://github.com/restorecommerce/protos#protos-acs",
    "@restorecommerce/resource-base-interface": "0.0.4",
    "@restorecommerce/service-config": "^0.1.6",
    "aws-sdk": "^2.711.0",
    "koa-multer": "^1.0.2",
    "memorystream": "^0.3.1",
    "multer-s3": "^2.9.0",
    "s3-upload-stream": "^1.0.7",
    "uuid": "^8.2.0"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.157",
    "@types/mocha": "^7.0.2",
    "@types/node": "^14.0.19",
    "@typescript-eslint/eslint-plugin": "^3.6.0",
    "@typescript-eslint/eslint-plugin-tslint": "^3.6.0",
    "@typescript-eslint/parser": "^3.6.0",
    "coveralls": "^3.1.0",
    "cross-env": "^7.0.2",
    "eslint": "^7.4.0",
    "eslint-plugin-prefer-arrow-functions": "^3.0.1",
    "grpc-mock": "^0.7.0",
    "mocha": "^8.0.1",
    "nodemon": "^2.0.4",
    "npm-run-all": "^4.1.5",
    "nyc": "^15.1.0",
    "rimraf": "^3.0.2",
    "should": "^13.2.3",
    "sleep": "^6.2.0",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.6"
  },
  "scripts": {
    "start": "npm run createtopics && node lib/start.js",
    "dev": "cross-env NODE_ENV=development npm run createtopics && nodemon --watch './src/**/*.ts' --exec ts-node src/start.ts",
    "pretest": "npm run build && npm run tsctests && npm run createtopics",
    "test": "npm run lint && nyc npm run mocha",
    "lint": "eslint src --ext .ts",
    "mocha": "cross-env NODE_ENV=test mocha --timeout 15000 -R spec test/test.js --full-trace --exit",
    "tsctests": "tsc -d -p tsconfig.test.json",
    "test-debug": "cross-env NODE_ENV=test mocha -R spec test/test.js --inspect-brk",
    "coveralls": "nyc report --reporter=text-lcov | coveralls",
    "createtopics": "node setupTopics.js io.restorecommerce.command",
    "build:tsc": "tsc -d",
    "build:clean": "rimraf lib",
    "build": "npm-run-all lint build:clean build:tsc",
    "healthcheck": "node lib/healthcheck.js"
  },
  "engines": {
    "node": ">= 12.0.0"
  }
}
